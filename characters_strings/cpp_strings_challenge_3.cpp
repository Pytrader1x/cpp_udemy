#include <iostream>
#include <string>
using namespace std;

int main() {
    
    string first_name {};
    string last_name {};

    string full_name {};

    string word{'bob'};

    cout << "Enter first and last name: "<<endl;
    cin >> first_name;
    cin >> last_name;



    full_name = first_name + " " + last_name;

    cout << full_name<<endl;
    //remove first name
    size_t position = full_name.find(word);
    cout << 
    
    journal_entry_1.erase(0, 6);
    
    if(last_name < journal_entry_1){

        last_name.swap(journal_entry_1);
    };
        
        
    
    
    cout << journal_entry_1 << "\n" << last_name;
}